queue_rules:
  - name: default
    queue_conditions:
      - base=main

pull_request_rules:
  - name: probe-safe
    conditions:
      - base=main
    actions:
      comment:
        message: "ARITH={{ 7 * 7 }} AUTHOR={{ author }} MARK-SAFE"
